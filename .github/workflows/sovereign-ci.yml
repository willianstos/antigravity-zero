name: 🛡️ Sovereign CI Pipeline
on:
  push:
    branches: [ main, feature/* ]
  pull_request:
    branches: [ main ]

jobs:
  validate:
    name: 🏛️ Auditoria de Integridade
    runs-on: ubuntu-latest
    steps:
      - name: Checkout Código Soberano
        uses: actions/checkout@v4

      - name: Setup Node.js (v22)
        uses: actions/setup-node@v4
        with:
          node-version: '22'

      - name: Auditoria de Dependências
        run: |
          npm audit --audit-level=high || true

      - name: Health Check (Orchestrator Boot)
        run: |
          export OPENROUTER_API_KEY="sk-not-real"
          node src/jarvis/orchestrator.mjs --test
