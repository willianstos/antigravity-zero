# 🔍 Protocolo de Descoberta Ubuntu (Soberana 2026) 🦅

Para garantir o melhor resultado, o Jarvis DEVE saber o que está instalado no sistema antes de propor ou executar uma solução.

## 🛠️ Ferramentas de Reconhecimento
- **Pesquisa de Binários**: `which <tool>` ou `whereis <tool>`.
- **Pesquisa de Capacidade**: `glxinfo | grep OpenGL` (Gráficos), `nvidia-smi` (GPU), `lscpu` (Proc).
- **Pesquisa de Automação**: `dpkg -l | grep -E "scrot|xdotool|ffmpeg|imagemagick"`.
- **Monitoramento**: `top -b -n 1` ou `bpython` para REPLs técnicos.

## 🚀 O que há de melhor (Best-of-Ubuntu 2026)
- **Imagem**: Use `ImageMagick` (`convert`) para processar evidências visuais antes do reporte.
- **Audio**: Use `aplay` para feedback sonoro no laboratório.
- **Network**: Use `tailscale status` para validar a Mesh Network Sovereing.
- **Scripting**: Priorize `Node.js v20+` para orquestração assíncrona e `Python 3.12` para IA pesada.

---
*Assinado: Jarvis v10.5 - Mestre do Kernel*
