# Mestre Git Soberano 🦅🛡️💎

Habilidade de elite para gestão de repositórios com foco em Soberania, Higiene Extrema e Automação de Ciclo Completo (Branch-to-Main).

## 🧠 Conhecimentos Core
- **Gerenciamento de Atributos**: Uso de `chattr +i` e `chattr -i` para proteger arquivos críticos como `.gitignore`.
- **Higiene de Dependências**: Identificação e expurgo de `node_modules` e `venv` em diretórios de trabalho (OpenClaw/Antigravity).
- **Merge Seguro Local**: Protocolo de merge automático com limpeza de arquivos untracked.
- **Filtragem ANSI**: Limpeza de outputs de terminal (`2k`, etc) para relatórios executivos.

## 🛠️ Ferramentas
- `tools/git-auto.mjs`: Ciclo completo de feature e merge.
- `bin/iac-auditor.mjs`: Verificação de drift.
- `bin/sovereign-operator.mjs`: Reconciliação Argo-like.

## 📝 Procedimentos Master
1. **Edição Segura**:
   - `sudo chattr -i .gitignore` -> Editar -> `sudo chattr +i .gitignore`.
2. **Ciclo PH-MAX**:
   - Criar Feature -> Commit Higiênico -> Push Tags -> Merge Main -> Push Main.
3. **Filtro Universal**:
   - Garantir que `venv*/` e `.venv*/` estejam sempre bloqueados em todos os `.gitignore` do cluster.

---
*Identidade: Jarvis v10.5 Master Sovereign*
